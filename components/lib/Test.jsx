import styled from 'styled-components';
// import PropTypes from 'prop-types';

const TestContainer = styled.div``;

export const Test = () => (
	<TestContainer>
		This is inside the test container.
	</TestContainer>
);
